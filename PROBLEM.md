# 문제 정의

## 배경

핸디즈(어반스테이)와 같은 위탁운영형 숙박 사업자는 자체 채널(어반스테이 앱)뿐 아니라
에어비앤비·야놀자·아고다 등 여러 OTA(Online Travel Agency)에 동일한 객실 재고를 동시에
노출한다. 각 OTA는 예약이 확정되면 웹훅(또는 폴링 API)으로 PMS에 예약 정보를 전달하고,
PMS는 이를 받아 내부 재고를 차감해야 다른 채널에서 같은 객실이 다시 팔리지 않는다.

이 흐름에는 구조적으로 두 가지 실패 지점이 있다.

1. **경쟁 조건(오버부킹)**: 서로 다른 채널에서 마지막 남은 객실에 대한 예약이 거의
   동시에 도착하면, 재고 확인과 차감 사이의 시간차(race condition)로 인해 두 예약이
   모두 성공 처리될 수 있다. 실제로 다중 채널을 운영하는 숙박 플랫폼에서 반복적으로
   보고되는 유형의 문제다.
2. **웹훅의 불완전한 전달(유실/중복)**: 웹훅은 네트워크 특성상 유실되거나(수신 시스템
   다운, 타임아웃) 중복 전달(발신측 재시도)될 수 있다. 유실되면 예약이 시스템에
   반영되지 않아 "OTA에는 예약이 있는데 PMS에는 없는" 상황이 생기고, 중복되면 같은
   예약이 두 번 처리되어 재고가 이중으로 차감된다.

두 문제 모두 고객이 현장에서 "예약이 없다"거나 "방이 없다"는 상황으로 직결되며,
운영 인력이 적은(무인/저인력) 운영 모델에서는 사람이 즉시 개입해 바로잡기 어렵다는
점에서 더 치명적이다.

## 이번 과제에서 푸는 문제

> **여러 채널에서 동시에 들어오는 예약 이벤트를, 재고 정합성을 깨뜨리지 않고
> (오버부킹 없이) 안전하게 수신·반영하는 백엔드 서비스를 만든다.**

구체적으로 아래 3가지를 보장하는 것이 목표다.

- 동시에 여러 요청이 마지막 재고를 두고 경쟁해도 정확히 하나만 성공한다.
- 같은 예약 웹훅이 중복 전달되어도 재고가 두 번 차감되지 않는다.
- 예약이 확정된 뒤 필요한 후속 처리(도어락 발급, 정산 트리거 등)가 일시적으로
  실패하더라도 유실되지 않고 재시도되며, 끝내 실패하면 별도로 추적 가능하다.

## 이번 과제에서 풀지 않는 문제 (Out of Scope)

시간 내에 깊이 있게 검증 가능한 범위로 좁히기 위해 아래는 의도적으로 제외한다.
(각 항목의 배제 이유는 [DESIGN.md](./DESIGN.md)의 "범위 판단" 절 참고)

- 실제 OTA API/웹훅 연동 (요청 스펙만 모사한 자체 API로 시뮬레이션)
- 프론트엔드/운영자 어드민 화면
- 실제 도어락·정산 시스템 연동 (다운스트림 처리는 로그/스텁으로 시뮬레이션)
- 연박(체크인~체크아웃 여러 날) 재고 계산, 요금 계산, 인증/인가
- 다중 인스턴스(수평 확장) 환경에서의 애플리케이션 레벨 분산락
- **가정**: 같은 `(channel, externalReservationId)`로 재전송되는 웹훅은 항상 같은
  객실타입·날짜를 담고 있다고 가정한다. 재전송인데 페이로드가 달라지는 경우(OTA
  측 데이터 오류로 간주)의 실제 동작은 순차/동시 두 케이스로 나뉜다.
  - **순차적으로 재전송되는 경우** (일반적인 케이스): `ReservationService.reserve()`는
    먼저 (다른/존재하지 않을 수 있는) 재고 행의 락을 획득한 뒤 멱등성 체크를
    수행하므로, 두 번째 요청은 insert를 시도하기도 전에 기존 예약을 찾아
    `ALREADY_PROCESSED`(200)를 반환하며 끝난다. 만약 페이로드가 가리키는
    재고 행 자체가 없다면 `ROOM_NOT_FOUND`(404)가 먼저 발생할 수도 있다.
    어느 쪽이든 예약 유니크 제약(insert)까지 도달하지 않는다.
  - **진짜 동시(concurrent)로 재전송되는 경우**: 서로 다른 재고 행을 잠그는 두
    요청이 동시에 들어와 각자 멱등성 체크까지는 통과했지만, 나중에 insert하는
    쪽이 `(channel, externalReservationId)` 유니크 제약에 걸린다. 이 경우에만
    실제로 제약 위반이 발생하며, 그 결과 API는 500 대신 409
    `DUPLICATE_CONFLICT`를 반환하도록 방어한다. 그 이상의 데이터 정합성 복구는
    다루지 않는다 (근거: 재요청은 정의상 같은 예약을 가리켜야 하며, 다르다면 이는
    OTA 쪽의 별도 데이터 오류이지 이 서비스가 흡수할 문제가 아니라고 판단)

## 참고 자료

- [OTA-호텔 관리 시스템 통합으로 오버부킹을 방지하는 방법](https://www.smartorder.ai/resources/ko/blog/how-to-avoid-overbookings-with-ota-pms-integration-ko/)
- [포트원 V2 - 웹훅 재시도, 어디까지 고도화해 보셨나요?](https://developers.portone.io/blog/posts/2024-02/v2-webhook)
- [선착순 티켓 예매의 동시성 문제: 잠금으로 안전하게 처리하기](https://tecoble.techcourse.co.kr/post/2023-08-16-concurrency-managing/)
- 핸디즈 CTO 인터뷰: ["이 호텔 운영사는 왜 CTO를 뽑았나"](https://byline.network/2024/06/cto1/) —
  "예약자 명단에 이름이 없거나, 명단에는 있는데 빈 방이 없는" 문제를 회사가 직접 언급함
